{% extends '::base.html.twig' %}

{% block body %}
<form action="{{ path('user_dashboard') }}" method="post" {{ form_enctype(userLogin) }} >
    {{ form_start(userLogin) }}
    {{ form_widget(userLogin) }}
    {{ form_end(userLogin) }}
</form>
{% endblock %}